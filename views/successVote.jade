extends layout

block content
  .container
    .row.col-md-offset-3.col-md-6
      h1= pollQuestion
        .well
          body
            h2 Thanks for Voting!
            h4 You voted for #{vote}
            h4.ul#render-votes
              // each val, index in pollOptions
              //   li #{index} have #{val} votes currently.

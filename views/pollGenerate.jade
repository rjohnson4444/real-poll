extends layout

block content
  .container
    .row
      .well
        body
          h1= title
          p <strong>Voting Url:</strong> #[a(href="#{voteUrl}") #{voteUrl}]
          p <strong>View results at:</strong> #[a(href="#{resultsUrl}") #{resultsUrl}]

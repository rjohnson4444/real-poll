extends layout

block content
  body
    .container
      .row.col-md-offset-3.col-md-6
        h1.text-center= pollQuestion
        .well
          #close-poll-message
          h4.ul#admin-view
            each val, index in currentPollInfo
              li #{index} currently has #{val} votes.
        a().btn.btn-primary#close-poll Close Poll
        br
        br
        #connection-count
